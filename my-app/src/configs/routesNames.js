const { REACT_APP_BASE_PATH } = process.env;

export default {
  home: `${REACT_APP_BASE_PATH}`,
  projects: `${REACT_APP_BASE_PATH}projects`,
  about: `${REACT_APP_BASE_PATH}about`,
  contact: `${REACT_APP_BASE_PATH}contact`,
};
